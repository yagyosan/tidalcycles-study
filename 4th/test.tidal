do
  d1
    $ stack [
      -- sound "bd*8" # pan (slow 2 sine)
      -- sound "bd*8" # pan (slow 2 cosine)
      -- sound "bd*8" # pan (slow 2 square)
      -- sound "bd*8" # pan (slow 2 tri)
      -- sound "bd*8" # pan (slow 2 saw)
      -- sound "bd*8" # pan (slow 2 isaw)
      -- sound "bd*8" # pan (slow 2 rand)
      -- sound "bd*8" # pan (slow 2 (irand 2))
      -- sound "bd*8" # pan (slow 2 (choose [0, 0.5, 1, 1.5]))
      -- sound "bd*8" # pan (slow 2 (chooseBy "0 0.25 0.5 0.75" [0, 0.5, 1, 1.5]))
    ]

hush
